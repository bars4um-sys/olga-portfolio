source "https://rubygems.org"

gem "jekyll", "~> 4.4"

# При локальной разработке: bundle install --path vendor/bundle
# При сборке: bundle exec jekyll build
# Для live-режима: bundle exec jekyll serve --livereload --port 4000
